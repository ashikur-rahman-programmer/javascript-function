/**
 * Task-1
Take four parameters. Multiply the four numbers and then return the result


 */

function paramFunc(num1, num2, num3, num4) {
  let result = num1 * num2 * num3 * num4;
  return result;
}

console.log(paramFunc(2, 4, 8, 6));
